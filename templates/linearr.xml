<?xml version="1.0"?>
<Container version="2">
  <Name>linearr</Name>
  <Repository>ghcr.io/gillberg1111/linearr:latest</Repository>
  <Registry>https://github.com/gillberg1111/linearr</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/gillberg1111/linearr/issues</Support>
  <Project>https://github.com/gillberg1111/linearr</Project>
  <Overview>
    Linearr — the missing show sequencer for Plex AND Jellyfin.
    Automated round-robin rotation and chronological crossover alignment for your episodes (and their movies).

    Configure either Plex, Jellyfin, or both. With both configured, each new playlist gets a "Push to: Both / Plex / Jellyfin"
    picker, and "Both" playlists are mirrored to each server independently using each server's own library state.

    Two sort modes per playlist:
    - Rotation: round-robin across shows (Show A S01E01, Show B S01E01, Show A S01E02, ...).
    - Air Date: chronological across every show. Multi-part crossovers stay aligned via
      "Part 1" / "Pt. 2" / "(N)" title parsing — perfect for "evening TV" mixes of the
      three Law and Order series, the Chicago shows, etc.

    Pick season ranges per show, opt in to specials, optionally include associated movies
    (auto-detected from your movie libraries by title match — e.g. "Psych: The Movie" attaches
    to Psych). Optional "Unwatched only" filter excludes episodes you've already watched.
    Live AJAX preview means every change updates the episode list immediately — no page reloads.

    Flip any playlist between sort modes or toggle the unwatched filter any time; already-watched
    episodes stay put, the future portion rebuilds. Edits show up as native playlists in
    every client (Plex AND Jellyfin). Background scheduler prunes watched episodes (keeping
    the last few around if you fell asleep).

    Safety: this app NEVER deletes media files or library items from Plex or Jellyfin — only playlists.
  </Overview>
  <Category>MediaApp:Other Tools:</Category>
  <WebUI>http://[IP]:[PORT:5005]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/gillberg1111/linearr/main/templates/linearr.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/gillberg1111/linearr/main/images/icon-256.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Description>
    The missing show sequencer for Plex AND Jellyfin. Automated round-robin rotation and
    chronological crossover alignment for your episodes (and their movies).
    • Configure Plex, Jellyfin, or both — playlists mirror to whichever you target
    • Two sort modes per playlist: Rotation (round-robin) or Air Date (chronological)
    • Air Date mode aligns multi-part crossovers (Part 1 / Pt. 2 / (1)) across different shows
    • Per-show season range, optional specials, auto-detected associated movies (e.g. Psych: The Movie)
    • Per-playlist "Unwatched only" filter
    • Pinned "Selected" tray when picking shows
    • Live AJAX preview with 10/25/50/100/All pagination — no page reloads on config changes
    • Add / remove / reorder shows; switch sort mode or filter any time
    • Background prune of watched episodes (keeps a configurable fall-asleep buffer)
    • Native playlist on every targeted server — works in every Plex AND Jellyfin client
    • Never deletes media files or library items (hard runtime guards on both backends)
  </Description>

  <Config Name="WebUI Port" Target="5005" Default="5005" Mode="tcp" Description="Port for the rotator web UI." Type="Port" Display="always" Required="true" Mask="false">5005</Config>

  <Config Name="Appdata" Target="/data" Default="/mnt/user/appdata/linearr" Mode="rw" Description="Persistent state (SQLite DB + stable device id)." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/linearr</Config>

  <!-- Plex (set both to enable; leave blank for Jellyfin-only installs) -->
  <Config Name="PLEX_URL" Target="PLEX_URL" Default="" Mode="" Description="Plex server URL (use the LAN IP, not plex.tv). Leave blank to disable Plex. Example: http://192.168.1.100:32400" Type="Variable" Display="always" Required="false" Mask="false"></Config>

  <Config Name="PLEX_TOKEN" Target="PLEX_TOKEN" Default="" Mode="" Description="X-Plex-Token. See https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/  Leave blank to disable Plex." Type="Variable" Display="always" Required="false" Mask="true"></Config>

  <!-- Jellyfin (set all three to enable; leave blank for Plex-only installs) -->
  <Config Name="JELLYFIN_URL" Target="JELLYFIN_URL" Default="" Mode="" Description="Jellyfin server URL (LAN IP). Example: http://192.168.1.100:8096  Leave blank to disable Jellyfin." Type="Variable" Display="always" Required="false" Mask="false"></Config>

  <Config Name="JELLYFIN_USERNAME" Target="JELLYFIN_USERNAME" Default="" Mode="" Description="Jellyfin username. (Linearr authenticates as a user because the API-key path is broken on the playlist endpoints we need.)  Leave blank to disable Jellyfin." Type="Variable" Display="always" Required="false" Mask="false"></Config>

  <Config Name="JELLYFIN_PASSWORD" Target="JELLYFIN_PASSWORD" Default="" Mode="" Description="Jellyfin password. Held in memory only; the resulting access token is never written to disk.  Leave blank to disable Jellyfin." Type="Variable" Display="always" Required="false" Mask="true"></Config>

  <Config Name="WATCHED_KEEP" Target="WATCHED_KEEP" Default="2" Mode="" Description="How many recently-watched episodes to leave in each playlist as a fall-asleep buffer. Older watched episodes are pruned." Type="Variable" Display="always" Required="false" Mask="false">2</Config>

  <Config Name="PRUNE_INTERVAL_MINUTES" Target="PRUNE_INTERVAL_MINUTES" Default="10" Mode="" Description="How often the background prune + auto-sync sweep runs, in minutes." Type="Variable" Display="advanced" Required="false" Mask="false">10</Config>

  <Config Name="FRANCHISE_REFRESH_DAYS" Target="FRANCHISE_REFRESH_DAYS" Default="7" Mode="" Description="How often franchise definitions are refreshed from upstream sources, in days (default 7 = weekly)." Type="Variable" Display="advanced" Required="false" Mask="false">7</Config>

  <Config Name="AUTO_SYNC" Target="AUTO_SYNC" Default="true" Mode="" Description="When true (default), the scheduler also splices newly-aired episodes and new seasons into managed playlists every PRUNE_INTERVAL_MINUTES. Set false to lock playlists until you edit them manually." Type="Variable" Display="advanced" Required="false" Mask="false">true</Config>

  <Config Name="TV_LIBRARIES" Target="TV_LIBRARIES" Default="" Mode="" Description="Comma-separated TV library names to source shows from (e.g. 'TV Shows,Dropout TV'). Leave blank for all TV libraries on every configured backend." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>

  <Config Name="FLASK_SECRET" Target="FLASK_SECRET" Default="" Mode="" Description="Optional. Random string for Flask session cookies. Generate with: openssl rand -hex 32" Type="Variable" Display="advanced" Required="false" Mask="true"></Config>
</Container>
